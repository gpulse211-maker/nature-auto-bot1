- name: Debug secrets
  run: |
    echo "Checking secrets..."
    if [ -z "${{ secrets.FB_PAGE_ID }}" ]; then echo "FB_PAGE_ID MISSING"; fi
    if [ -z "${{ secrets.FB_ACCESS_TOKEN }}" ]; then echo "TOKEN MISSING"; fi
    if [ -z "${{ secrets.PEXELS_API }}" ]; then echo "PEXELS MISSING"; fi
